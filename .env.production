# 生产构建读取；Cloudflare Build 变量同名时优先覆盖本文件。
# 若线上挂在子路径，请设 PUBLIC_CANONICAL_ORIGIN=https://域名/pestcontrol（等）；
# 代码会在 SITE 仍为 *.pages.dev 或裸主域时，用 CANONICAL 对齐 sitemap。
PUBLIC_SITE_URL=https://rockwellpropertiesmaine.com
